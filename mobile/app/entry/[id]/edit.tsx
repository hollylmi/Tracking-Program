// Entry edit form — coming in next prompt
// Reuses the 5-step form from new.tsx
// Pre-populated with existing entry data

import { View, Text } from 'react-native'

export default function EntryEditScreen() {
  return <View />
}
